{% extends 'base.html.twig' %}

{% block title %}Inscription !{% endblock %}

{% block body %}
{#{{ form_start(form) }}#}
    {#{{ form_widget(form) }}#}



    {#<button class="btn btn-lg btn-primary" type="submit">#}
        {#Sign in#}
    {#</button>#}
{#{{ form_end(form) }}#}

    <h2 class="text-center  mb-5"> Inscris toi !</h2>
    <div class="container">
        {{ form_start (form) }}
        {{ form_widget (form) }}

        <div class="form-group row">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" value="Envoi de missive" class="btn btn-success">
            </div>
        </div>

        {{ form_end (form) }}
    </div>

{% endblock %}
